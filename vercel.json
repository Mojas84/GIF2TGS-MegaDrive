{
  "version": 2,
  "buildCommand": "pnpm build",
  "outputDirectory": "dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "functions": {
    "api/convert.py": {
      "maxDuration": 60,
      "includeFiles": ["scripts/**", "requirements.txt"]
    }
  }
}
